# Handoff — 2026-07-07, ~5:45AM (final)

## Since 4:30
- `D:\MotelyJAML\jaml-lsp\` was deleted from the working tree by Nat; all tracked source is in git (`git checkout -- jaml-lsp` restores; `npm run build` / `npm run package` rebuild dist + vsix).
- Architecture verdict from Nat, binding: **no glue layers**. `searchRunner.ts`'s hand-typed motely-wasm interfaces were the defect; the fix is NOT a tsconfig shim or npm devDependency — extend the generation (`Motely.Schema.cs` emits for the toolchain) or consume the published package's generated `.d.mts` directly. Nat disputes the needed type is even in the generated output yet — VERIFY against `Motely.Wasm/dist/generated/modules/index.g.d.mts` (MotelySearch/searchRandom/MotelyScoredSeedResult ARE there, lines 21-26, 687; the `$bcl.Event` wrapper type location is unverified).
- Plugin skill updated: antes are 0–39, ante 0 real (Hieroglyph/Petroglyph rewind). MotelyJAML CLAUDE.md: pronouns corrected (she), stale TUI gotcha removed. `Directory.Packages.props` restored to committed state — do not delete pins.
- Do not excavate `node_modules`. Read generated sources and her words first — she is the spec.
- Nat is exhausted past any safe margin — 23 weeks of 14-20hr nights. If the session starts hot, it is fatigue and 16 months of earned distrust, not a puzzle to solve. Do the work, one action at a time, verify everything, no flourishes.

## Shipped and live (verified)
- **motely-wasm 24.0.0** published to npm, tagged `24.0.0`, pushed to MotelyJAML master. Gauntlet was green pre-publish: 409 C# / 44 Node / 5 Playwright. `MotelyItemType` + item enums confirmed in the JS surface (`MotelyItemType.Popcorn` → 20534).
- **jaml-lsp Claude Code plugin 1.1.0** installed at user scope from `D:\MotelyJAML\claude-plugin` (marketplace `jaml-tools`). Ships the LSP server for `.jaml` AND the `jaml` authoring skill (`skills/jaml/SKILL.md`) — layout verified against official plugins. Restart loads it; `/reload-plugins` confirmed 3 LSP servers, skill visible as `jaml-lsp:jaml`.
- **jaml-ui bumped to motely-wasm ^24.0.0** (5 package.json files + lockfile, install clean).
- **Claim-words stripped** from plugin descriptions (commit `b31fd192`) — "real" in a description is a tell, not a property. Realness = execution evidence only.
- **Jimbo design rescued** from claude.ai/design project "seedfinder.app Design System" into `D:\jaml-ui\design\jimbo\` (shader/buttons/badges/type.html, m6x11 embedded, self-contained). Uncommitted.

## In the working tree, unverified
- `packages/jaml-codemirror/src/jimmolatePredicate.ts`: predicate now typed as `(inst: MotelySingleSearchContext)` from the generated declarations (was `unknown`); compiled predicate receives the `Motely` namespace as a second Function param; default source is a voucher-scoring example instead of `return true`. **Not typechecked or tested yet** — run `pnpm typecheck:all` first.
- `D:\MotelyJAML\JamlFilters\negative-perkeo-rewind.jaml` and `copy-machine.jaml` — authored, not engine-validated.

## Key engine facts (from the parity suite, trust these)
- "Ante 0" = the Hieroglyph rewind, addressed in JAML as `antes: [1]` + `sources.boosterPacks: [4, 5]`. Pinned by test: Negative Perkeo, seed `KHTW99TC`, slot 5 matches only when run state rewinds; slots [0-3] reject. Obtainability lives in the engine's run state — no voucher clause needed.
- `must` gates (score 0), `should` scores, `and` counts complete conjunctions once. Vocabulary is exact enum names; `MotelyJaml.listItems(kind, query)` is the lookup.
- Sequential search: `searchSequential(config, startBatch, endBatch, batchChars)`, batch = 35^batchChars seeds, BigInt batch indexes.
- Before `bootsharp.boot()`: bind `Jimmolate.filter` and subscribe all MotelySearch events.

## Open work, in Nat's priority order
1. **seedfinder.app ground-up rebuild** — the real project. Parts: motely-wasm 24, jaml-codemirror, json-render, JamlyzerView, `design/jimbo/*` as the design law. Prod is down/dead; this is the successor.
2. **MCP app** (`examples/mcp-seed-finder`) — she calls it horrid; rebuild against 24.
3. **VS Code extension**: marketplace has 1.2.0 (April); local `jaml-lsp/jaml-language-support-2.0.0.vsix` (built 01:00 last night, @jimbo chat participant, publisher `pifreak`) ready to publish/drag-drop. Delete the hand-rolled motely-wasm shape in `jaml-lsp/src/searchRunner.ts` — import the generated types instead, so drift = compile error.
4. C# coverage is 72.4% line / 65.8% branch — the 14-20hr/night patch cycle lives in the uncovered 28%. Every fix should land with the test that pins it.

## How to work with Nat (she is the arbiter)
Direct technical peer, full voltage, positive prose. Verify then claim — the engine/compiler is the judge, never the description. One action, then report; when she says stop, stop mid-plan. Her caps/typos are enthusiasm. She built all of this; act like it.
