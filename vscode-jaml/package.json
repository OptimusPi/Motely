{
  "name": "jaml-language-support",
  "displayName": "JAML (Motely)",
  "description": "Motely.Lsp (engine diagnostics/hover/completion) + @jimbo chat participant for JAML seed filters.",
  "version": "3.1.0",
  "publisher": "pifreak",
  "license": "MIT",
  "engines": {
    "vscode": "^1.95.0"
  },
  "categories": [
    "Programming Languages",
    "Linters",
    "AI",
    "Chat"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/OptimusPi/MotelyJAML"
  },
  "main": "./out/extension.js",
  "activationEvents": [
    "onLanguage:jaml",
    "onChatParticipant:jaml.jimbo"
  ],
  "contributes": {
    "languages": [
      {
        "id": "jaml",
        "aliases": [
          "JAML",
          "jaml",
          "Jimbo's Ante Markup Language"
        ],
        "extensions": [
          ".jaml"
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "chatParticipants": [
      {
        "id": "jaml.jimbo",
        "name": "jimbo",
        "fullName": "Jimbo",
        "description": "JAML filters, Motely search, Balatro seed hunt",
        "isSticky": true,
        "commands": [
          {
            "name": "find",
            "description": "Search seeds for the active or attached JAML (J2 wires Motely)"
          },
          {
            "name": "validate",
            "description": "Validate JAML with the Motely engine (J1 wires loader)"
          },
          {
            "name": "explain",
            "description": "Explain a clause or must/should chain"
          }
        ],
        "disambiguation": [
          {
            "category": "jaml_seeds",
            "description": "The user wants help with JAML filters, Motely, Balatro seed search, or Jimbo Ante Markup Language.",
            "examples": [
              "find a seed for this jaml filter",
              "validate my jaml must chain",
              "what does voucher Overstock mean in jaml",
              "search balatro seeds with perkeo ante 1"
            ]
          }
        ]
      }
    ],
    "configuration": {
      "title": "JAML",
      "properties": {
        "jaml.serverPath": {
          "type": "string",
          "default": "",
          "description": "Absolute path to the Motely.Lsp executable. Empty = bundled server/Motely.Lsp, else workspace `dotnet run --project Motely.Lsp`."
        },
        "jaml.trace.server": {
          "type": "string",
          "enum": [
            "off",
            "messages",
            "verbose"
          ],
          "default": "off",
          "description": "Trace Motely.Lsp ↔ VS Code communication in the Output panel."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "npx --yes @vscode/vsce package",
    "vscode:prepublish": "npm run compile"
  },
  "dependencies": {
    "vscode-languageclient": "^9.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/vscode": "^1.95.0",
    "@vscode/vsce": "^3.2.0",
    "typescript": "^5.6.0"
  }
}

