# Plain conditions (local only)

1. Copy `conditions.template.json` → `conditions.json` in this folder.
2. Fill system/user prefixes yourself. **Do not paste into chat. Do not commit.**
3. Seal: `python ../src/seal.py`
4. Delete or shred `conditions.json` if agents will browse this tree.

This folder is gitignored except this note.
