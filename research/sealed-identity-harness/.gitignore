# Plaintext NEVER enters git or agent context.
plain/**
!plain/.gitkeep
!plain/README.md
*.key
.env
.env.*
results/**
!results/.gitkeep
# Sealed blobs may be committed if key is private; still prefer private sealed/
# Uncomment next line to keep ciphertext local only:
# sealed/**
