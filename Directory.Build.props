<Project>
  <PropertyGroup>
    <!-- Prevent stale Visual Studio fallback package folders from leaking into restore/build. -->
    <DisableImplicitNuGetFallbackFolder>true</DisableImplicitNuGetFallbackFolder>
  </PropertyGroup>
</Project>
