{
  "name": "motely-wasm-release-smoke",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Live release smoke test against the published motely-wasm npm package. The dependency version is set at runtime by build.ps1's release-smoke action so the test always exercises the artifact that was just shipped.",
  "dependencies": {
    "motely-wasm": "^13.0.2"
  },
  "scripts": {
    "test": "node release-smoke.mjs"
  }
}
