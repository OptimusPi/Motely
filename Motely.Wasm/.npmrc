# GitHub Packages for @rewaffle/* (Bootsharp.FileSystem JS half).
# Token is an env var only — never paste a PAT into this file.
#   export NODE_AUTH_TOKEN=ghp_...   # classic PAT: read:packages
@rewaffle:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
