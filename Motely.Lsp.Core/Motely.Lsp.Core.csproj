<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <IsPackable>false</IsPackable>
    <!-- Protocol-free language brain: diagnostics, hover, completion — off the engine's own
         grammar (JamlSchema, JamlConfigLoader). Motely.Lsp (stdio) and any
         future shell call in here; the grammar stays authored once, in C#. -->
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Motely\Motely.csproj" />
  </ItemGroup>

</Project>
