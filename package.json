{
  "name": "motelyjaml-native-packages",
  "private": true,
  "description": "One script builds two npm packages (motely-wasm + motely-node). Version: Directory.Packages.props → MotelyVersion.",
  "scripts": {
    "pack": "node build.mjs --pack",
    "build:wasm": "node build.mjs wasm",
    "build:node": "node build.mjs node",
    "build": "node build.mjs"
  }
}
